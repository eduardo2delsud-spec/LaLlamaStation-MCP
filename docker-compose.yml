version: "3.8"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: mcp-ollama-motor
    ports:
      - "11434:11434"
    # habilita acceso a GPU para Ollama; este es el contenedor que realmente hace inferencia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - mcp-network
    restart: always

  mcp-server:
    build:
      context: ./ollama-mcp-server
      dockerfile: Dockerfile
    container_name: mcp-server-app
    volumes:
      - ollama_data:/root/.ollama:ro
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - ollama
    environment:
      - OLLAMA_URL=http://ollama:11434
      - APP_PORT=${APP_PORT:-3000}
      - API_KEY=${API_KEY:-super-secret-mcp-key}
      - NGROK_CONTAINER_NAME=mcp-ngrok-tunnel
    ports:
      - "${APP_PORT:-3000}:${APP_PORT:-3000}"
    # habilita acceso a GPU si el host y Docker lo soportan
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    # alternativa simplificada en Compose 3.8+:
    # gpus: all
    networks:
      - mcp-network
    restart: always

  ngrok:
    image: ngrok/ngrok:latest
    container_name: mcp-ngrok-tunnel
    depends_on:
      - mcp-server
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command:
      - http
      - mcp-server:${APP_PORT:-3000}
    networks:
      - mcp-network
    restart: "no"

  mcp-frontend:
    build:
      context: ./mcp-frontend
      dockerfile: Dockerfile
    container_name: mcp-frontend-app
    ports:
      - "8080:80"
    environment:
      # inside docker the backend is reachable by service name
      - VITE_API_URL=http://mcp-server:${APP_PORT:-3000}
      - VITE_SOCKET_URL=http://mcp-server:${APP_PORT:-3000}
    networks:
      - mcp-network
    depends_on:
      - mcp-server
    restart: always

networks:
  mcp-network:
    driver: bridge

volumes:
  ollama_data:
