# Variables de entorno para la aplicación de frontend
# Copia este archivo a `.env` y modifica según tu entorno (no se debe versionar).

# URL base del API (servicio mcp-server)
VITE_API_URL=http://backend:3000

# URL WebSocket para señales en tiempo real (si se usa)
VITE_SOCKET_URL=http://backend:3000
